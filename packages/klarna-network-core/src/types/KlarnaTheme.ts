/** The visual theme applied to Klarna UI components. */
export enum KlarnaTheme {
  /** Light appearance. */
  Light = 'light',
  /** Dark appearance. */
  Dark = 'dark',
  /** Follows the system appearance setting. */
  Automatic = 'automatic',
}
